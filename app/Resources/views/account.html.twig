{% extends 'base.html.twig' %}

{% block body %}

    {{ form_start(form) }}
    {{ form_rest(form) }}
    <button type="submit">Envoyer</button>
    {{ form_end(form) }}

    <a href="{{ path('change_password') }}">{{ 'change_password'|trans }}</a>

{% endblock %}


