{% extends 'base.html.twig' %}

{% block body %}

    <h2>{{ 'change_password'|trans }}</h2>
    {{ form_start(form) }}
    {{ form_rest(form) }}
    <button type="submit">Envoyer</button>
    {{ form_end(form) }}

{% endblock %}

